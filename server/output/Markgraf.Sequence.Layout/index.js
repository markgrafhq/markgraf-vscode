// Generated by purs version 0.15.16
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var rowY = function (layout) {
    return function (row) {
        return layout.bodyTop + (Data_Int.toNumber(row) + 0.5) * layout.metrics.rowHeight;
    };
};
var defaultMetrics = {
    padding: 24.0,
    headerHeight: 36.0,
    headerWidth: 120.0,
    columnSpacing: 160.0,
    rowHeight: 36.0,
    topGap: 24.0,
    bottomGap: 24.0
};
var layoutSequence = function (diagram) {
    var headerBottom = defaultMetrics.padding + defaultMetrics.headerHeight;
    var bodyTop = headerBottom + defaultMetrics.topGap;
    var bodyBottom = bodyTop + Data_Int.toNumber(max(1)(diagram.totalRows)) * defaultMetrics.rowHeight;
    var height = bodyBottom + defaultMetrics.bottomGap + defaultMetrics.padding;
    var assignColumn = function (i) {
        return function (lifeline) {
            return {
                lifeline: lifeline,
                x: defaultMetrics.padding + defaultMetrics.headerWidth / 2.0 + Data_Int.toNumber(i) * defaultMetrics.columnSpacing
            };
        };
    };
    var columns = Data_Array.mapWithIndex(assignColumn)(diagram.lifelines);
    var rightmostX = (function () {
        var v = Data_Array.last(columns);
        if (v instanceof Data_Maybe.Nothing) {
            return defaultMetrics.padding;
        };
        if (v instanceof Data_Maybe.Just) {
            return v.value0.x + defaultMetrics.headerWidth / 2.0;
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Layout (line 77, column 16 - line 79, column 46): " + [ v.constructor.name ]);
    })();
    var width = rightmostX + defaultMetrics.padding;
    return {
        metrics: defaultMetrics,
        columns: columns,
        width: width,
        height: height,
        headerTop: defaultMetrics.padding,
        headerBottom: headerBottom,
        bodyTop: bodyTop,
        bodyBottom: bodyBottom,
        diagram: diagram
    };
};
var columnX = function (layout) {
    return function (i) {
        var v = Data_Array.index(layout.columns)(i);
        if (v instanceof Data_Maybe.Just) {
            return v.value0.x;
        };
        if (v instanceof Data_Maybe.Nothing) {
            return layout.metrics.padding;
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Layout (line 89, column 20 - line 91, column 36): " + [ v.constructor.name ]);
    };
};
export {
    layoutSequence,
    defaultMetrics,
    columnX,
    rowY
};
//# sourceMappingURL=index.js.map
