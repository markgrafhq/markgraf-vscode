// Generated by purs version 0.15.16
import * as Control_Applicative from "../Control.Applicative/index.js";
import * as Control_Bind from "../Control.Bind/index.js";
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Unit from "../Data.Unit/index.js";
import * as Markgraf_Animation_Render_Op from "../Markgraf.Animation.Render.Op/index.js";
import * as Run from "../Run/index.js";
var append = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var clamp = /* #__PURE__ */ Data_Ord.clamp(Data_Ord.ordNumber);
var pure = /* #__PURE__ */ Control_Applicative.pure(Run.applicativeRun);
var clamp1 = /* #__PURE__ */ Data_Ord.clamp(Data_Ord.ordInt);
var when = /* #__PURE__ */ Control_Applicative.when(Run.applicativeRun);
var discard = /* #__PURE__ */ Control_Bind.discard(Control_Bind.discardUnit)(Run.bindRun);
var bind = /* #__PURE__ */ Control_Bind.bind(Run.bindRun);
var foldM = /* #__PURE__ */ Data_Array.foldM(Run.monadRun);
var trackColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(64);
var solid = function (c) {
    return {
        color: c,
        flat: true
    };
};
var rectPath = function (r) {
    return append(Markgraf_Animation_Render_Op.pathMove(r.x)(r.y))(append(Markgraf_Animation_Render_Op.pathLine(r.x + r.w)(r.y))(append(Markgraf_Animation_Render_Op.pathLine(r.x + r.w)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathLine(r.x)(r.y + r.h))(Markgraf_Animation_Render_Op.pathClose))));
};
var pointInRect = function (r) {
    return function (x) {
        return function (y) {
            return x >= r.x && (x <= r.x + r.w && (y >= r.y && y <= r.y + r.h));
        };
    };
};
var playheadShadow = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(0)(0)(0)(64);
var playheadOutline = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(0)(0)(0)(102);
var playheadColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(255);
var playedColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(242);
var playIcon = function (b) {
    return append(Markgraf_Animation_Render_Op.pathMove(b.x + b.w * 0.36)(b.y + b.h * 0.22))(append(Markgraf_Animation_Render_Op.pathLine(b.x + b.w * 0.74)(b.y + b.h * 0.5))(append(Markgraf_Animation_Render_Op.pathLine(b.x + b.w * 0.36)(b.y + b.h * 0.78))(Markgraf_Animation_Render_Op.pathClose)));
};
var keyfPassed = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(0)(0)(0)(153);
var keyfAhead = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(230);
var kappa = 0.5522847498307936;
var roundedRectPath = function (r) {
    return function (rr0) {
        if (r.w <= 0.0 || r.h <= 0.0) {
            return [  ];
        };
        if (Data_Boolean.otherwise) {
            var rr = min(rr0)(min(r.w / 2.0)(r.h / 2.0));
            var k = kappa * rr;
            return append(Markgraf_Animation_Render_Op.pathMove(r.x + rr)(r.y))(append(Markgraf_Animation_Render_Op.pathLine((r.x + r.w) - rr)(r.y))(append(Markgraf_Animation_Render_Op.pathCube(((r.x + r.w) - rr) + k)(r.y)(r.x + r.w)((r.y + rr) - k)(r.x + r.w)(r.y + rr))(append(Markgraf_Animation_Render_Op.pathLine(r.x + r.w)((r.y + r.h) - rr))(append(Markgraf_Animation_Render_Op.pathCube(r.x + r.w)(((r.y + r.h) - rr) + k)(((r.x + r.w) - rr) + k)(r.y + r.h)((r.x + r.w) - rr)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathLine(r.x + rr)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathCube((r.x + rr) - k)(r.y + r.h)(r.x)(((r.y + r.h) - rr) + k)(r.x)((r.y + r.h) - rr))(append(Markgraf_Animation_Render_Op.pathLine(r.x)(r.y + rr))(append(Markgraf_Animation_Render_Op.pathCube(r.x)((r.y + rr) - k)((r.x + rr) - k)(r.y)(r.x + rr)(r.y))(Markgraf_Animation_Render_Op.pathClose)))))))));
        };
        throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 350, column 1 - line 350, column 42): " + [ r.constructor.name, rr0.constructor.name ]);
    };
};
var pauseIcon = function (b) {
    return function (dpr) {
        return append(roundedRectPath({
            x: b.x + b.w * 0.33,
            y: b.y + b.h * 0.26,
            w: b.w * 0.1,
            h: b.h * 0.48
        })(1.5 * dpr))(roundedRectPath({
            x: b.x + b.w * 0.57,
            y: b.y + b.h * 0.26,
            w: b.w * 0.1,
            h: b.h * 0.48
        })(1.5 * dpr));
    };
};
var hoverColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(102);
var fwdIcon = function (b) {
    return function (dpr) {
        return append(append(Markgraf_Animation_Render_Op.pathMove(b.x + b.w * 0.26)(b.y + b.h * 0.24))(append(Markgraf_Animation_Render_Op.pathLine(b.x + b.w * 0.64)(b.y + b.h * 0.5))(append(Markgraf_Animation_Render_Op.pathLine(b.x + b.w * 0.26)(b.y + b.h * 0.76))(Markgraf_Animation_Render_Op.pathClose))))(roundedRectPath({
            x: b.x + b.w * 0.69,
            y: b.y + b.h * 0.28,
            w: b.w * 7.0e-2,
            h: b.h * 0.44
        })(1.0 * dpr));
    };
};
var diamondPath = function (cx) {
    return function (cy) {
        return function (half) {
            return append(Markgraf_Animation_Render_Op.pathMove(cx)(cy + half))(append(Markgraf_Animation_Render_Op.pathLine(cx + half)(cy))(append(Markgraf_Animation_Render_Op.pathLine(cx)(cy - half))(append(Markgraf_Animation_Render_Op.pathLine(cx - half)(cy))(Markgraf_Animation_Render_Op.pathClose))));
        };
    };
};
var cursorOnBar = function (bar) {
    return function (dpr) {
        return function (x) {
            return function (y) {
                var pad = 10.0 * dpr;
                return x >= bar.x - pad && (x <= bar.x + bar.w + pad && (y >= bar.y - pad && y <= bar.y + bar.h + pad));
            };
        };
    };
};
var controlsLayout = function (i) {
    var csm = 16.0 * i.dpr;
    var ch = 36.0 * i.dpr;
    var bs = 24.0 * i.dpr;
    var bp = 8.0 * i.dpr;
    var bh = 8.0 * i.dpr;
    var bg = 4.0 * i.dpr;
    var bbi = 20.0 * i.dpr;
    var chrome = {
        x: csm,
        y: i.frameH - bbi - ch,
        w: i.frameW - 2.0 * csm,
        h: ch
    };
    var btnY = chrome.y + (ch - bs) / 2.0;
    var repeatBtn = {
        x: (chrome.x + chrome.w) - bp - bs,
        y: btnY,
        w: bs,
        h: bs
    };
    var barRight = repeatBtn.x - 1.5 * bp;
    var back = {
        x: chrome.x + bp,
        y: btnY,
        w: bs,
        h: bs
    };
    var play = {
        x: back.x + bs + bg,
        y: btnY,
        w: bs,
        h: bs
    };
    var fwd = {
        x: play.x + bs + bg,
        y: btnY,
        w: bs,
        h: bs
    };
    var barLeft = fwd.x + fwd.w + 1.5 * bp;
    var bar = {
        x: barLeft,
        y: chrome.y + (ch - bh) / 2.0,
        w: max(0.0)(barRight - barLeft),
        h: bh
    };
    return {
        chrome: chrome,
        bar: bar,
        back: back,
        play: play,
        fwd: fwd,
        repeatBtn: repeatBtn
    };
};
var clamp01 = /* #__PURE__ */ clamp(0.0)(1.0);
var circlePath = function (cx) {
    return function (cy) {
        return function (radius) {
            if (radius <= 0.0) {
                return [  ];
            };
            if (Data_Boolean.otherwise) {
                var c = kappa * radius;
                return append(Markgraf_Animation_Render_Op.pathMove(cx + radius)(cy))(append(Markgraf_Animation_Render_Op.pathCube(cx + radius)(cy + c)(cx + c)(cy + radius)(cx)(cy + radius))(append(Markgraf_Animation_Render_Op.pathCube(cx - c)(cy + radius)(cx - radius)(cy + c)(cx - radius)(cy))(append(Markgraf_Animation_Render_Op.pathCube(cx - radius)(cy - c)(cx - c)(cy - radius)(cx)(cy - radius))(append(Markgraf_Animation_Render_Op.pathCube(cx + c)(cy - radius)(cx + radius)(cy - c)(cx + radius)(cy))(Markgraf_Animation_Render_Op.pathClose)))));
            };
            throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 415, column 1 - line 415, column 49): " + [ cx.constructor.name, cy.constructor.name, radius.constructor.name ]);
        };
    };
};
var playedPath = function (r) {
    if (r.w <= 0.0 || r.h <= 0.0) {
        return [  ];
    };
    if (r.w <= r.h / 2.0) {
        return circlePath(r.x + r.h / 2.0)(r.y + r.h / 2.0)(r.h / 2.0);
    };
    if (Data_Boolean.otherwise) {
        var rr = r.h / 2.0;
        var k = kappa * rr;
        return append(Markgraf_Animation_Render_Op.pathMove(r.x + rr)(r.y))(append(Markgraf_Animation_Render_Op.pathLine(r.x + r.w)(r.y))(append(Markgraf_Animation_Render_Op.pathLine(r.x + r.w)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathLine(r.x + rr)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathCube((r.x + rr) - k)(r.y + r.h)(r.x)(r.y + rr + k)(r.x)(r.y + rr))(append(Markgraf_Animation_Render_Op.pathCube(r.x)((r.y + rr) - k)((r.x + rr) - k)(r.y)(r.x + rr)(r.y))(Markgraf_Animation_Render_Op.pathClose))))));
    };
    throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 398, column 1 - line 398, column 27): " + [ r.constructor.name ]);
};
var chromeColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(0)(0)(0)(153);
var capsulePath = function (r) {
    if (r.w <= 0.0 || r.h <= 0.0) {
        return [  ];
    };
    if (r.w <= r.h) {
        return circlePath(r.x + r.w / 2.0)(r.y + r.h / 2.0)(r.w / 2.0);
    };
    if (Data_Boolean.otherwise) {
        var rr = r.h / 2.0;
        var k = kappa * rr;
        return append(Markgraf_Animation_Render_Op.pathMove(r.x + rr)(r.y))(append(Markgraf_Animation_Render_Op.pathLine((r.x + r.w) - rr)(r.y))(append(Markgraf_Animation_Render_Op.pathCube(((r.x + r.w) - rr) + k)(r.y)(r.x + r.w)((r.y + rr) - k)(r.x + r.w)(r.y + rr))(append(Markgraf_Animation_Render_Op.pathCube(r.x + r.w)(r.y + rr + k)(((r.x + r.w) - rr) + k)(r.y + r.h)((r.x + r.w) - rr)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathLine(r.x + rr)(r.y + r.h))(append(Markgraf_Animation_Render_Op.pathCube((r.x + rr) - k)(r.y + r.h)(r.x)(r.y + rr + k)(r.x)(r.y + rr))(append(Markgraf_Animation_Render_Op.pathCube(r.x)((r.y + rr) - k)((r.x + rr) - k)(r.y)(r.x + rr)(r.y))(Markgraf_Animation_Render_Op.pathClose)))))));
    };
    throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 370, column 1 - line 370, column 28): " + [ r.constructor.name ]);
};
var buttonOnFg = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(10)(10)(12)(240);
var buttonOn = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(230);
var buttonHover = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(38);
var buttonFg = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(242);
var bufferedPath = function (r) {
    return function (barRect) {
        if (r.w >= barRect.w - 0.5) {
            return capsulePath(barRect);
        };
        if (Data_Boolean.otherwise) {
            return playedPath(r);
        };
        throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 393, column 1 - line 393, column 37): " + [ r.constructor.name, barRect.constructor.name ]);
    };
};
var bufferedColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgba(255)(255)(255)(130);
var backIcon = function (b) {
    return function (dpr) {
        return append(roundedRectPath({
            x: b.x + b.w * 0.24,
            y: b.y + b.h * 0.28,
            w: b.w * 7.0e-2,
            h: b.h * 0.44
        })(1.0 * dpr))(append(Markgraf_Animation_Render_Op.pathMove(b.x + b.w * 0.74)(b.y + b.h * 0.24))(append(Markgraf_Animation_Render_Op.pathLine(b.x + b.w * 0.74)(b.y + b.h * 0.76))(append(Markgraf_Animation_Render_Op.pathLine(b.x + b.w * 0.36)(b.y + b.h * 0.5))(Markgraf_Animation_Render_Op.pathClose))));
    };
};
var arc270Path = function (cx) {
    return function (cy) {
        return function (r) {
            var c = kappa * r;
            return append(Markgraf_Animation_Render_Op.pathMove(cx)(cy - r))(append(Markgraf_Animation_Render_Op.pathCube(cx + c)(cy - r)(cx + r)(cy - c)(cx + r)(cy))(append(Markgraf_Animation_Render_Op.pathCube(cx + r)(cy + c)(cx + c)(cy + r)(cx)(cy + r))(Markgraf_Animation_Render_Op.pathCube(cx - c)(cy + r)(cx - r)(cy + c)(cx - r)(cy))));
        };
    };
};
var scrubberRender = function (input) {
    if (input.frameCount <= 0) {
        return pure(Data_Unit.unit);
    };
    if (input.revealPhase < 5.0e-3) {
        return pure(Data_Unit.unit);
    };
    if (Data_Boolean.otherwise) {
        var progress = (function () {
            if (input.frameCount <= 1) {
                return 0.0;
            };
            if (Data_Boolean.otherwise) {
                return clamp01(Data_Int.toNumber(input.tick) / Data_Int.toNumber(input.frameCount - 1 | 0));
            };
            throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 161, column 7 - line 163, column 86): " + [  ]);
        })();
        var layout = controlsLayout(input);
        var hoverPad = 4.0 * input.dpr;
        var bufferedProgress = (function () {
            if (input.frameCount <= 1) {
                return 1.0;
            };
            if (Data_Boolean.otherwise) {
                return clamp01(Data_Int.toNumber(input.buffered) / Data_Int.toNumber(input.frameCount));
            };
            throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 169, column 7 - line 171, column 84): " + [  ]);
        })();
        var bufferedW = layout.bar.w * bufferedProgress;
        var buffered = {
            h: layout.bar.h,
            x: layout.bar.x,
            y: layout.bar.y,
            w: bufferedW
        };
        var headR = layout.bar.h * 0.85;
        var headX = layout.bar.x + layout.bar.w * progress;
        var headY = layout.bar.y + layout.bar.h / 2.0;
        var playedW = layout.bar.w * progress;
        var played = {
            h: layout.bar.h,
            x: layout.bar.x,
            y: layout.bar.y,
            w: playedW
        };
        var alpha = clamp01(input.revealPhase);
        var fade = function (c) {
            return {
                b: c.b,
                g: c.g,
                r: c.r,
                a: clamp1(0)(255)(Data_Int.round(Data_Int.toNumber(c.a) * alpha))
            };
        };
        var drawHover = (function () {
            var hx = clamp(layout.bar.x)(layout.bar.x + layout.bar.w)(input.mouseX);
            var hr = {
                x: hx - input.dpr / 2.0,
                y: layout.bar.y - hoverPad,
                w: input.dpr,
                h: layout.bar.h + 2.0 * hoverPad
            };
            return Markgraf_Animation_Render_Op.fillPath(rectPath(hr))(solid(fade(hoverColor)));
        })();
        var drawHoverHighlight = function (b) {
            return function (forceShow) {
                return when(forceShow || pointInRect(b)(input.mouseX)(input.mouseY))(Markgraf_Animation_Render_Op.fillPath(circlePath(b.x + b.w / 2.0)(b.y + b.h / 2.0)(b.w * 0.48))(solid(fade((function () {
                    if (forceShow) {
                        return buttonOn;
                    };
                    return buttonHover;
                })()))));
            };
        };
        var drawButton = function (b) {
            return function (iconPath) {
                return discard(drawHoverHighlight(b)(false))(function () {
                    return Markgraf_Animation_Render_Op.fillPath(iconPath)(solid(fade(buttonFg)));
                });
            };
        };
        var drawRepeat = function (b) {
            return function (active) {
                return function (dpr) {
                    return discard(drawHoverHighlight(b)(active))(function () {
                        var fgCol = fade((function () {
                            if (active) {
                                return buttonOnFg;
                            };
                            return buttonFg;
                        })());
                        var cx = b.x + b.w / 2.0;
                        var cy = b.y + b.h / 2.0;
                        var rr = b.w * 0.28;
                        var sw = 1.6 * dpr;
                        return discard(Markgraf_Animation_Render_Op.strokePath(arc270Path(cx)(cy)(rr))({
                            color: fgCol,
                            width: sw,
                            lineJoin: Markgraf_Animation_Render_Op.RoundJoin.value,
                            lineCap: Markgraf_Animation_Render_Op.RoundCap.value
                        }))(function () {
                            var ah = b.w * 0.13;
                            return Markgraf_Animation_Render_Op.fillPath(append(Markgraf_Animation_Render_Op.pathMove(cx - rr - ah * 0.7)(cy))(append(Markgraf_Animation_Render_Op.pathLine((cx - rr) + ah * 0.7)(cy))(append(Markgraf_Animation_Render_Op.pathLine(cx - rr)(cy - ah))(Markgraf_Animation_Render_Op.pathClose))))(solid(fgCol));
                        });
                    });
                };
            };
        };
        var stepKey = function (v) {
            return function (fi) {
                if (fi <= 0 || fi >= input.frameCount) {
                    return pure(Data_Unit.unit);
                };
                if (Data_Boolean.otherwise) {
                    var tx = Data_Int.toNumber(fi) / Data_Int.toNumber(input.frameCount - 1 | 0);
                    var px = layout.bar.x + layout.bar.w * tx;
                    var half = layout.bar.h / 2.0;
                    var cy = layout.bar.y + layout.bar.h / 2.0;
                    var col = (function () {
                        var $42 = tx <= progress;
                        if ($42) {
                            return keyfPassed;
                        };
                        return keyfAhead;
                    })();
                    return Markgraf_Animation_Render_Op.fillPath(diamondPath(px)(cy)(half))(solid(fade(col)));
                };
                throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 212, column 7 - line 221, column 67): " + [ v.constructor.name, fi.constructor.name ]);
            };
        };
        return discard(Markgraf_Animation_Render_Op.fillPath(roundedRectPath(layout.chrome)(12.0 * input.dpr))(solid(fade(chromeColor))))(function () {
            return discard(drawButton(layout.back)(backIcon(layout.back)(input.dpr)))(function () {
                return discard(drawButton(layout.play)((function () {
                    if (input.paused) {
                        return playIcon(layout.play);
                    };
                    return pauseIcon(layout.play)(input.dpr);
                })()))(function () {
                    return discard(drawButton(layout.fwd)(fwdIcon(layout.fwd)(input.dpr)))(function () {
                        return discard(drawRepeat(layout.repeatBtn)(input.looping)(input.dpr))(function () {
                            return discard(Markgraf_Animation_Render_Op.fillPath(capsulePath(layout.bar))(solid(fade(trackColor))))(function () {
                                return discard(when(bufferedW > 0.0)(Markgraf_Animation_Render_Op.fillPath(bufferedPath(buffered)(layout.bar))(solid(fade(bufferedColor)))))(function () {
                                    return discard(when(playedW > 0.0)(Markgraf_Animation_Render_Op.fillPath(playedPath(played))(solid(fade(playedColor)))))(function () {
                                        return bind(foldM(stepKey)(Data_Unit.unit)(input.keyframes))(function () {
                                            return discard(when(cursorOnBar(layout.bar)(input.dpr)(input.mouseX)(input.mouseY))(drawHover))(function () {
                                                return discard(Markgraf_Animation_Render_Op.fillPath(circlePath(headX)(headY + 1.0 * input.dpr)(headR))(solid(fade(playheadShadow))))(function () {
                                                    return discard(Markgraf_Animation_Render_Op.fillPath(circlePath(headX)(headY)(headR))(solid(fade(playheadColor))))(function () {
                                                        return Markgraf_Animation_Render_Op.strokePath(circlePath(headX)(headY)(headR))({
                                                            color: fade(playheadOutline),
                                                            width: 1.0 * input.dpr,
                                                            lineJoin: Markgraf_Animation_Render_Op.RoundJoin.value,
                                                            lineCap: Markgraf_Animation_Render_Op.RoundCap.value
                                                        });
                                                    });
                                                });
                                            });
                                        });
                                    });
                                });
                            });
                        });
                    });
                });
            });
        });
    };
    throw new Error("Failed pattern match at Markgraf.Animation.Render.Scrubber (line 116, column 1 - line 116, column 67): " + [ input.constructor.name ]);
};
export {
    scrubberRender,
    controlsLayout,
    cursorOnBar,
    pointInRect
};
//# sourceMappingURL=index.js.map
