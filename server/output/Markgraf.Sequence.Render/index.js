// Generated by purs version 0.15.16
import * as Control_Bind from "../Control.Bind/index.js";
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_Foldable from "../Data.Foldable/index.js";
import * as Data_Functor from "../Data.Functor/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Map_Internal from "../Data.Map.Internal/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Newtype from "../Data.Newtype/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Set from "../Data.Set/index.js";
import * as Data_String_Common from "../Data.String.Common/index.js";
import * as Data_Tuple from "../Data.Tuple/index.js";
import * as Markgraf_Animation_Render_Op from "../Markgraf.Animation.Render.Op/index.js";
import * as Markgraf_Animation_Render_Recording from "../Markgraf.Animation.Render.Recording/index.js";
import * as Markgraf_Graph from "../Markgraf.Graph/index.js";
import * as Markgraf_Sequence_Layout from "../Markgraf.Sequence.Layout/index.js";
import * as Markgraf_Sequence_Model from "../Markgraf.Sequence.Model/index.js";
import * as Run from "../Run/index.js";
var append = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var insert = /* #__PURE__ */ Data_Map_Internal.insert(Data_Ord.ordInt);
var lookup = /* #__PURE__ */ Data_Map_Internal.lookup(Data_Ord.ordInt);
var $$delete = /* #__PURE__ */ Data_Map_Internal["delete"](Data_Ord.ordInt);
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var elem = /* #__PURE__ */ Data_Array.elem(Data_Eq.eqInt);
var eq = /* #__PURE__ */ Data_Eq.eq(Data_Eq.eqInt);
var insert1 = /* #__PURE__ */ Data_Set.insert(Data_Ord.ordInt);
var member = /* #__PURE__ */ Data_Set.member(Data_Ord.ordInt);
var map = /* #__PURE__ */ Data_Functor.map(Data_Functor.functorArray);
var un = /* #__PURE__ */ Data_Newtype.un();
var discard = /* #__PURE__ */ Control_Bind.discard(Control_Bind.discardUnit)(Run.bindRun);
var for_ = /* #__PURE__ */ Data_Foldable.for_(Run.applicativeRun)(Data_Foldable.foldableArray);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var max1 = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var notEq = /* #__PURE__ */ Data_Eq.notEq(Markgraf_Sequence_Model.eqMessageKind);
var fromFoldable = /* #__PURE__ */ Data_Set.fromFoldable(Data_Foldable.foldableArray)(Data_Ord.ordInt);
var foldl = /* #__PURE__ */ Data_Foldable.foldl(Data_Foldable.foldableArray);
var white = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(255)(255)(255);
var undersideOffset = 5.0;
var textColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(26)(26)(26);
var tagFill = {
    color: /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(232)(232)(232),
    flat: true
};
var shadowColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(232)(232)(232);
var selfLoopWidth = 36.0;
var roundedRect = function (x0) {
    return function (y0) {
        return function (x1) {
            return function (y1) {
                return function (r) {
                    return append(Markgraf_Animation_Render_Op.pathMove(x0 + r)(y0))(append(Markgraf_Animation_Render_Op.pathLine(x1 - r)(y0))(append(Markgraf_Animation_Render_Op.pathQuad(x1)(y0)(x1)(y0 + r))(append(Markgraf_Animation_Render_Op.pathLine(x1)(y1 - r))(append(Markgraf_Animation_Render_Op.pathQuad(x1)(y1)(x1 - r)(y1))(append(Markgraf_Animation_Render_Op.pathLine(x0 + r)(y1))(append(Markgraf_Animation_Render_Op.pathQuad(x0)(y1)(x0)(y1 - r))(append(Markgraf_Animation_Render_Op.pathLine(x0)(y0 + r))(append(Markgraf_Animation_Render_Op.pathQuad(x0)(y0)(x0 + r)(y0))(Markgraf_Animation_Render_Op.pathClose)))))))));
                };
            };
        };
    };
};
var pushOpen = function (col) {
    return function (row) {
        return function (s) {
            return {
                spans: s.spans,
                returnRows: s.returnRows,
                stack: Data_Array.snoc(s.stack)(col),
                openedAt: insert(col)(row)(s.openedAt)
            };
        };
    };
};
var popOne = function (row) {
    return function (s) {
        var v = Data_Array.last(s.stack);
        if (v instanceof Data_Maybe.Nothing) {
            return s;
        };
        if (v instanceof Data_Maybe.Just) {
            var opened = Data_Maybe.fromMaybe(row)(lookup(v.value0)(s.openedAt));
            return {
                returnRows: s.returnRows,
                stack: Data_Maybe.fromMaybe([  ])(Data_Array.init(s.stack)),
                openedAt: $$delete(v.value0)(s.openedAt),
                spans: Data_Array.snoc(s.spans)({
                    col: v.value0,
                    fromRow: opened,
                    toRow: max(opened)(row)
                })
            };
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 213, column 16 - line 223, column 10): " + [ v.constructor.name ]);
    };
};
var popUntil = function ($copy_pred) {
    return function ($copy_row) {
        return function ($copy_s) {
            var $tco_var_pred = $copy_pred;
            var $tco_var_row = $copy_row;
            var $tco_done = false;
            var $tco_result;
            function $tco_loop(pred, row, s) {
                var v = Data_Array.last(s.stack);
                if (v instanceof Data_Maybe.Nothing) {
                    $tco_done = true;
                    return s;
                };
                if (v instanceof Data_Maybe.Just) {
                    if (pred(v.value0)) {
                        $tco_done = true;
                        return s;
                    };
                    if (Data_Boolean.otherwise) {
                        $tco_var_pred = pred;
                        $tco_var_row = row;
                        $copy_s = popOne(row)(s);
                        return;
                    };
                };
                throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 201, column 23 - line 205, column 52): " + [ v.constructor.name ]);
            };
            while (!$tco_done) {
                $tco_result = $tco_loop($tco_var_pred, $tco_var_row, $copy_s);
            };
            return $tco_result;
        };
    };
};
var popAll = function ($copy_row) {
    return function ($copy_s) {
        var $tco_var_row = $copy_row;
        var $tco_done = false;
        var $tco_result;
        function $tco_loop(row, s) {
            var v = Data_Array.last(s.stack);
            if (v instanceof Data_Maybe.Nothing) {
                $tco_done = true;
                return s;
            };
            if (v instanceof Data_Maybe.Just) {
                $tco_var_row = row;
                $copy_s = popOne(row)(s);
                return;
            };
            throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 208, column 16 - line 210, column 38): " + [ v.constructor.name ]);
        };
        while (!$tco_done) {
            $tco_result = $tco_loop($tco_var_row, $copy_s);
        };
        return $tco_result;
    };
};
var processMsg = function (s) {
    return function (msg) {
        var handleReceiver = function (st) {
            if (elem(msg.toCol)(st.stack)) {
                var st11 = popUntil(eq(msg.toCol))(msg.row - 1 | 0)(popOne(msg.row)(st));
                return {
                    openedAt: st11.openedAt,
                    spans: st11.spans,
                    stack: st11.stack,
                    returnRows: insert1(msg.row)(st11.returnRows)
                };
            };
            if (Data_Boolean.otherwise) {
                return pushOpen(msg.toCol)(msg.row)(st);
            };
            throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 190, column 3 - line 198, column 48): " + [ st.constructor.name ]);
        };
        var ensureFromOnTop = function (st) {
            if (elem(msg.fromCol)(st.stack)) {
                return popUntil(eq(msg.fromCol))(msg.row - 1 | 0)(st);
            };
            if (Data_Boolean.otherwise) {
                return pushOpen(msg.fromCol)(msg.row)(popAll(msg.row - 1 | 0)(st));
            };
            throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 186, column 3 - line 188, column 73): " + [ st.constructor.name ]);
        };
        return handleReceiver(ensureFromOnTop(s));
    };
};
var stepWithFrameEnd = function (ends) {
    return function (s) {
        return function (msg) {
            var s1 = processMsg(s)(msg);
            var $54 = member(msg.row)(ends);
            if ($54) {
                return popAll(msg.row)(s1);
            };
            return s1;
        };
    };
};
var messageStroke = /* #__PURE__ */ (function () {
    return {
        color: Markgraf_Animation_Render_Op.rgb(26)(26)(26),
        width: 1.5,
        lineJoin: Markgraf_Animation_Render_Op.MiterJoin.value,
        lineCap: Markgraf_Animation_Render_Op.RoundCap.value
    };
})();
var messageColor = /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(26)(26)(26);
var lifelineStroke = /* #__PURE__ */ (function () {
    return {
        color: Markgraf_Animation_Render_Op.rgb(130)(130)(130),
        width: 1.0,
        lineJoin: Markgraf_Animation_Render_Op.MiterJoin.value,
        lineCap: Markgraf_Animation_Render_Op.ButtCap.value
    };
})();
var joinLabels = function (labels) {
    return Data_String_Common.joinWith("\x0a")(map(un(Markgraf_Graph.Label))(labels));
};
var headerStroke = /* #__PURE__ */ (function () {
    return {
        color: Markgraf_Animation_Render_Op.rgb(26)(26)(26),
        width: 1.5,
        lineJoin: Markgraf_Animation_Render_Op.MiterJoin.value,
        lineCap: Markgraf_Animation_Render_Op.ButtCap.value
    };
})();
var headerFill = {
    color: /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(244)(244)(244),
    flat: true
};
var hasBarAt = function (acts) {
    return function (col) {
        return function (row) {
            return Data_Array.any(function (a) {
                return a.col === col && (a.fromRow <= row && row <= a.toRow);
            })(acts);
        };
    };
};
var fragmentStroke = /* #__PURE__ */ (function () {
    return {
        color: Markgraf_Animation_Render_Op.rgb(90)(90)(90),
        width: 1.0,
        lineJoin: Markgraf_Animation_Render_Op.MiterJoin.value,
        lineCap: Markgraf_Animation_Render_Op.ButtCap.value
    };
})();
var fragLabel = function (v) {
    return "par";
};
var emptyAct = {
    stack: [  ],
    openedAt: Data_Map_Internal.empty,
    spans: [  ],
    returnRows: Data_Set.empty
};
var dividerStroke = /* #__PURE__ */ (function () {
    return {
        color: Markgraf_Animation_Render_Op.rgb(150)(150)(150),
        width: 1.0,
        lineJoin: Markgraf_Animation_Render_Op.MiterJoin.value,
        lineCap: Markgraf_Animation_Render_Op.ButtCap.value
    };
})();
var drawDivider = function (layout) {
    return function (x0) {
        return function (x1) {
            return function (row) {
                var y = Markgraf_Sequence_Layout.rowY(layout)(row) - layout.metrics.rowHeight / 2.0;
                var line = append(Markgraf_Animation_Render_Op.pathMove(x0)(y))(Markgraf_Animation_Render_Op.pathLine(x1)(y));
                return Markgraf_Animation_Render_Op.strokePath(line)(dividerStroke);
            };
        };
    };
};
var drawFragment = function (layout) {
    return function (frag) {
        var top = Markgraf_Sequence_Layout.rowY(layout)(frag.fromRow) - layout.metrics.rowHeight / 2.0 - 6.0;
        var x0 = Markgraf_Sequence_Layout.columnX(layout)(frag.fromCol) - 16.0;
        var tag = append(Markgraf_Animation_Render_Op.pathMove(x0)(top))(append(Markgraf_Animation_Render_Op.pathLine(x0 + 38.0)(top))(append(Markgraf_Animation_Render_Op.pathLine(x0 + 32.0)(top + 14.0))(append(Markgraf_Animation_Render_Op.pathLine(x0)(top + 14.0))(Markgraf_Animation_Render_Op.pathClose))));
        var x1 = Markgraf_Sequence_Layout.columnX(layout)(frag.toCol) + 16.0;
        var bot = Markgraf_Sequence_Layout.rowY(layout)(max(frag.toRow)(frag.fromRow + 1 | 0) - 1 | 0) + layout.metrics.rowHeight / 2.0 + 6.0;
        var box = append(Markgraf_Animation_Render_Op.pathMove(x0)(top))(append(Markgraf_Animation_Render_Op.pathLine(x1)(top))(append(Markgraf_Animation_Render_Op.pathLine(x1)(bot))(append(Markgraf_Animation_Render_Op.pathLine(x0)(bot))(Markgraf_Animation_Render_Op.pathClose))));
        return discard(Markgraf_Animation_Render_Op.strokePath(box)(fragmentStroke))(function () {
            return discard(Markgraf_Animation_Render_Op.fillStrokePath(tag)(tagFill)(fragmentStroke))(function () {
                return discard(Markgraf_Animation_Render_Op.drawText({
                    x: x0 + 6.0,
                    y: top + 7.0,
                    content: fragLabel(frag.kind),
                    font: {
                        family: "Inter",
                        size: 11.0,
                        weight: 700
                    },
                    color: textColor,
                    align: Markgraf_Animation_Render_Op.AlignLeft.value,
                    baseline: Markgraf_Animation_Render_Op.BaselineMiddle.value
                }))(function () {
                    return for_(frag.regionDividers)(drawDivider(layout)(x0)(x1));
                });
            });
        });
    };
};
var dashOnX = 6.0;
var dashOn = 6.0;
var dashOffX = 4.0;
var dashRight = function (x) {
    return function (b) {
        if (x >= b) {
            return [  ];
        };
        if (Data_Boolean.otherwise) {
            return append([ new Data_Tuple.Tuple(x, min(b)(x + dashOnX)) ])(dashRight(x + dashOnX + dashOffX)(b));
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 330, column 1 - line 330, column 58): " + [ x.constructor.name, b.constructor.name ]);
    };
};
var dashOff = 4.0;
var dashSegments = function (y0) {
    return function (y1) {
        if (y1 <= y0) {
            return [  ];
        };
        if (Data_Boolean.otherwise) {
            var go = function (y) {
                if (y >= y1) {
                    return [  ];
                };
                if (Data_Boolean.otherwise) {
                    return append([ new Data_Tuple.Tuple(y, min(y1)(y + dashOn)) ])(go(y + dashOn + dashOff));
                };
                throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 132, column 7 - line 134, column 80): " + [ y.constructor.name ]);
            };
            return go(y0);
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 127, column 1 - line 127, column 61): " + [ y0.constructor.name, y1.constructor.name ]);
    };
};
var drawLifelineColumn = function (layout) {
    return function (col) {
        var startY = layout.headerTop + layout.metrics.headerHeight + Data_Int.toNumber(col.lifeline.createdAt) * layout.metrics.rowHeight + 4.0;
        var endY = (function () {
            if (col.lifeline.destroyedAt instanceof Data_Maybe.Just) {
                return Markgraf_Sequence_Layout.rowY(layout)(col.lifeline.destroyedAt.value0);
            };
            if (col.lifeline.destroyedAt instanceof Data_Maybe.Nothing) {
                return layout.bodyBottom;
            };
            throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 123, column 10 - line 125, column 33): " + [ col.lifeline.destroyedAt.constructor.name ]);
        })();
        return for_(dashSegments(startY)(endY))(function (v) {
            return Markgraf_Animation_Render_Op.strokePath(append(Markgraf_Animation_Render_Op.pathMove(col.x)(v.value0))(Markgraf_Animation_Render_Op.pathLine(col.x)(v.value1)))(lifelineStroke);
        });
    };
};
var dashLeft = function (x) {
    return function (b) {
        if (x <= b) {
            return [  ];
        };
        if (Data_Boolean.otherwise) {
            return append([ new Data_Tuple.Tuple(x, max1(b)(x - dashOnX)) ])(dashLeft(x - dashOnX - dashOffX)(b));
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 335, column 1 - line 335, column 57): " + [ x.constructor.name, b.constructor.name ]);
    };
};
var dashSegmentsX = function (a) {
    return function (b) {
        if (a === b) {
            return [  ];
        };
        if (a < b) {
            return dashRight(a)(b);
        };
        if (Data_Boolean.otherwise) {
            return dashLeft(a)(b);
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 324, column 1 - line 324, column 62): " + [ a.constructor.name, b.constructor.name ]);
    };
};
var cornerRadius = 6.0;
var drawHeader = function (layout) {
    return function (col) {
        var top = layout.headerTop + Data_Int.toNumber(col.lifeline.createdAt) * layout.metrics.rowHeight;
        var halfW = layout.metrics.headerWidth / 2.0;
        var bot = top + layout.metrics.headerHeight;
        var rect = roundedRect(col.x - halfW)(top)(col.x + halfW)(bot)(cornerRadius);
        var shadow = roundedRect(col.x - halfW)(top + undersideOffset)(col.x + halfW)(bot + undersideOffset)(cornerRadius);
        return discard(Markgraf_Animation_Render_Op.fillStrokePath(shadow)({
            color: shadowColor,
            flat: true
        })(headerStroke))(function () {
            return discard(Markgraf_Animation_Render_Op.fillStrokePath(rect)(headerFill)(headerStroke))(function () {
                return Markgraf_Animation_Render_Op.drawText({
                    x: col.x,
                    y: top + layout.metrics.headerHeight / 2.0,
                    content: col.lifeline.label,
                    font: {
                        family: "Inter",
                        size: 14.0,
                        weight: 600
                    },
                    color: textColor,
                    align: Markgraf_Animation_Render_Op.AlignCenter.value,
                    baseline: Markgraf_Animation_Render_Op.BaselineMiddle.value
                });
            });
        });
    };
};
var closeAll = function ($copy_row) {
    return function ($copy_s) {
        var $tco_var_row = $copy_row;
        var $tco_done = false;
        var $tco_result;
        function $tco_loop(row, s) {
            var v = Data_Array.last(s.stack);
            if (v instanceof Data_Maybe.Nothing) {
                $tco_done = true;
                return s;
            };
            if (v instanceof Data_Maybe.Just) {
                $tco_var_row = row;
                $copy_s = popOne(row)(s);
                return;
            };
            throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 232, column 18 - line 234, column 40): " + [ v.constructor.name ]);
        };
        while (!$tco_done) {
            $tco_result = $tco_loop($tco_var_row, $copy_s);
        };
        return $tco_result;
    };
};
var callStack = function (layout) {
    var msgs = Data_Array.filter(function (m) {
        return notEq(m.kind)(Markgraf_Sequence_Model.SelfMsg.value);
    })(layout.diagram.messages);
    var lastRow = (function () {
        var v = Data_Array.last(layout.diagram.messages);
        if (v instanceof Data_Maybe.Just) {
            return v.value0.row;
        };
        if (v instanceof Data_Maybe.Nothing) {
            return 0;
        };
        throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 156, column 13 - line 158, column 17): " + [ v.constructor.name ]);
    })();
    var ends = fromFoldable(layout.diagram.frameEndRows);
    var run = closeAll(lastRow)(foldl(stepWithFrameEnd(ends))(emptyAct)(msgs));
    return run;
};
var arrowSize = 10.0;
var drawSelfMessage = function (layout) {
    return function (msg) {
        var y = Markgraf_Sequence_Layout.rowY(layout)(msg.row);
        var x = Markgraf_Sequence_Layout.columnX(layout)(msg.fromCol);
        var topY = y - layout.metrics.rowHeight * 0.3;
        var rightX = x + selfLoopWidth;
        var botY = y + layout.metrics.rowHeight * 0.3;
        var loop = append(Markgraf_Animation_Render_Op.pathMove(x)(topY))(append(Markgraf_Animation_Render_Op.pathLine(rightX)(topY))(append(Markgraf_Animation_Render_Op.pathLine(rightX)(botY))(Markgraf_Animation_Render_Op.pathLine(x)(botY))));
        var baseX = x + arrowSize;
        var head = append(Markgraf_Animation_Render_Op.pathMove(x)(botY))(append(Markgraf_Animation_Render_Op.pathLine(baseX)(botY - arrowSize / 2.0))(append(Markgraf_Animation_Render_Op.pathLine(baseX)(botY + arrowSize / 2.0))(Markgraf_Animation_Render_Op.pathClose)));
        return discard(Markgraf_Animation_Render_Op.strokePath(loop)(messageStroke))(function () {
            return discard(Markgraf_Animation_Render_Op.fillPath(head)({
                color: messageColor,
                flat: true
            }))(function () {
                return Markgraf_Animation_Render_Op.drawText({
                    x: x + selfLoopWidth + 6.0,
                    y: y,
                    content: joinLabels(msg.labels),
                    font: {
                        family: "Inter",
                        size: 12.0,
                        weight: 400
                    },
                    color: textColor,
                    align: Markgraf_Animation_Render_Op.AlignLeft.value,
                    baseline: Markgraf_Animation_Render_Op.BaselineMiddle.value
                });
            });
        });
    };
};
var activationWidth = 12.0;
var drawSyncMessage = function (layout) {
    return function (acts) {
        return function (isReturn) {
            return function (msg) {
                var y = (function () {
                    if (isReturn) {
                        return Markgraf_Sequence_Layout.rowY(layout)(msg.row) + layout.metrics.rowHeight * 0.5;
                    };
                    return Markgraf_Sequence_Layout.rowY(layout)(msg.row);
                })();
                var xToCenter = Markgraf_Sequence_Layout.columnX(layout)(msg.toCol);
                var xFromCenter = Markgraf_Sequence_Layout.columnX(layout)(msg.fromCol);
                var toOffset = (function () {
                    var $75 = hasBarAt(acts)(msg.toCol)(msg.row);
                    if ($75) {
                        return activationWidth / 2.0;
                    };
                    return 0.0;
                })();
                var goingRight = msg.toCol >= msg.fromCol;
                var fromOffset = (function () {
                    var $76 = hasBarAt(acts)(msg.fromCol)(msg.row);
                    if ($76) {
                        return activationWidth / 2.0;
                    };
                    return 0.0;
                })();
                var dir = (function () {
                    if (goingRight) {
                        return 1.0;
                    };
                    return -1.0;
                })();
                var xFrom = xFromCenter + dir * fromOffset;
                var xTo = xToCenter - dir * toOffset;
                var shaft = append(Markgraf_Animation_Render_Op.pathMove(xFrom)(y))(Markgraf_Animation_Render_Op.pathLine(xTo)(y));
                var baseX = xTo - dir * arrowSize;
                var drawArrowHead = (function () {
                    if (isReturn) {
                        return Markgraf_Animation_Render_Op.strokePath(append(Markgraf_Animation_Render_Op.pathMove(baseX)(y - arrowSize / 2.0))(append(Markgraf_Animation_Render_Op.pathLine(xTo)(y))(Markgraf_Animation_Render_Op.pathLine(baseX)(y + arrowSize / 2.0))))(messageStroke);
                    };
                    return Markgraf_Animation_Render_Op.fillPath(append(Markgraf_Animation_Render_Op.pathMove(xTo)(y))(append(Markgraf_Animation_Render_Op.pathLine(baseX)(y - arrowSize / 2.0))(append(Markgraf_Animation_Render_Op.pathLine(baseX)(y + arrowSize / 2.0))(Markgraf_Animation_Render_Op.pathClose))))({
                        color: messageColor,
                        flat: true
                    });
                })();
                return discard((function () {
                    if (isReturn) {
                        return for_(dashSegmentsX(xFrom)(xTo))(function (v) {
                            return Markgraf_Animation_Render_Op.strokePath(append(Markgraf_Animation_Render_Op.pathMove(v.value0)(y))(Markgraf_Animation_Render_Op.pathLine(v.value1)(y)))(messageStroke);
                        });
                    };
                    return Markgraf_Animation_Render_Op.strokePath(shaft)(messageStroke);
                })())(function () {
                    return discard(drawArrowHead)(function () {
                        return Markgraf_Animation_Render_Op.drawText({
                            x: (xFrom + xTo) / 2.0,
                            y: y - 6.0,
                            content: joinLabels(msg.labels),
                            font: {
                                family: "Inter",
                                size: 12.0,
                                weight: 400
                            },
                            color: textColor,
                            align: Markgraf_Animation_Render_Op.AlignCenter.value,
                            baseline: Markgraf_Animation_Render_Op.BaselineBottom.value
                        });
                    });
                });
            };
        };
    };
};
var drawMessage = function (layout) {
    return function (acts) {
        return function (returns) {
            return function (msg) {
                if (msg.kind instanceof Markgraf_Sequence_Model.SelfMsg) {
                    return drawSelfMessage(layout)(msg);
                };
                if (msg.kind instanceof Markgraf_Sequence_Model.Sync) {
                    return drawSyncMessage(layout)(acts)(member(msg.row)(returns))(msg);
                };
                throw new Error("Failed pattern match at Markgraf.Sequence.Render (line 268, column 39 - line 270, column 71): " + [ msg.kind.constructor.name ]);
            };
        };
    };
};
var activationStroke = /* #__PURE__ */ (function () {
    return {
        color: Markgraf_Animation_Render_Op.rgb(26)(26)(26),
        width: 1.25,
        lineJoin: Markgraf_Animation_Render_Op.RoundJoin.value,
        lineCap: Markgraf_Animation_Render_Op.ButtCap.value
    };
})();
var activationShadowFill = {
    color: /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(232)(232)(232),
    flat: true
};
var activationRadius = 3.0;
var activationFill = {
    color: /* #__PURE__ */ Markgraf_Animation_Render_Op.rgb(252)(252)(252),
    flat: true
};
var drawActivation = function (layout) {
    return function (act) {
        var x = Markgraf_Sequence_Layout.columnX(layout)(act.col);
        var top = Markgraf_Sequence_Layout.rowY(layout)(act.fromRow);
        var halfW = activationWidth / 2.0;
        var bot = Markgraf_Sequence_Layout.rowY(layout)(act.toRow) + layout.metrics.rowHeight * 0.5;
        var rect = roundedRect(x - halfW)(top)(x + halfW)(bot)(activationRadius);
        var shadow = roundedRect(x - halfW)(top + undersideOffset)(x + halfW)(bot + undersideOffset)(activationRadius);
        return discard(Markgraf_Animation_Render_Op.fillStrokePath(shadow)(activationShadowFill)(activationStroke))(function () {
            return Markgraf_Animation_Render_Op.fillStrokePath(rect)(activationFill)(activationStroke);
        });
    };
};
var renderSequence = function (layout) {
    var st = callStack(layout);
    return Markgraf_Animation_Render_Recording.runFrameOps(discard(Markgraf_Animation_Render_Op.setViewport({
        vx: 0.0,
        vy: 0.0,
        vw: layout.width,
        vh: layout.height
    }))(function () {
        return discard(Markgraf_Animation_Render_Op.clearBackground(white))(function () {
            return discard(for_(layout.columns)(drawHeader(layout)))(function () {
                return discard(for_(layout.columns)(drawLifelineColumn(layout)))(function () {
                    return discard(for_(st.spans)(drawActivation(layout)))(function () {
                        return discard(for_(layout.diagram.fragments)(drawFragment(layout)))(function () {
                            return for_(layout.diagram.messages)(drawMessage(layout)(st.spans)(st.returnRows));
                        });
                    });
                });
            });
        });
    }));
};
export {
    renderSequence
};
//# sourceMappingURL=index.js.map
