// Generated by purs version 0.15.16
import * as Control_Applicative from "../Control.Applicative/index.js";
import * as Data_Array from "../Data.Array/index.js";
import * as Data_Boolean from "../Data.Boolean/index.js";
import * as Data_Eq from "../Data.Eq/index.js";
import * as Data_EuclideanRing from "../Data.EuclideanRing/index.js";
import * as Data_Int from "../Data.Int/index.js";
import * as Data_Maybe from "../Data.Maybe/index.js";
import * as Data_Number from "../Data.Number/index.js";
import * as Data_Ord from "../Data.Ord/index.js";
import * as Data_Semigroup from "../Data.Semigroup/index.js";
import * as Data_Unit from "../Data.Unit/index.js";
import * as Markgraf_Animation_Render_Metrics from "../Markgraf.Animation.Render.Metrics/index.js";
import * as Markgraf_Animation_Render_Op from "../Markgraf.Animation.Render.Op/index.js";
import * as Markgraf_Graph from "../Markgraf.Graph/index.js";
import * as Markgraf_Whiteboard_Prng from "../Markgraf.Whiteboard.Prng/index.js";
import * as Run from "../Run/index.js";
var max = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordNumber);
var min = /* #__PURE__ */ Data_Ord.min(Data_Ord.ordNumber);
var clamp = /* #__PURE__ */ Data_Ord.clamp(Data_Ord.ordInt);
var max1 = /* #__PURE__ */ Data_Ord.max(Data_Ord.ordInt);
var div1 = /* #__PURE__ */ Data_EuclideanRing.div(Data_EuclideanRing.euclideanRingInt);
var elem = /* #__PURE__ */ Data_Array.elem(Data_Eq.eqInt);
var clamp1 = /* #__PURE__ */ Data_Ord.clamp(Data_Ord.ordNumber);
var append = /* #__PURE__ */ Data_Semigroup.append(Data_Semigroup.semigroupArray);
var lift = /* #__PURE__ */ Run.lift()({
    reflectSymbol: function () {
        return "render";
    }
})(Markgraf_Animation_Render_Op.functorRenderF);
var RunDraw = function (x) {
    return x;
};
var FlatLevel = /* #__PURE__ */ (function () {
    function FlatLevel() {

    };
    FlatLevel.value = new FlatLevel();
    return FlatLevel;
})();
var NestedLevel = /* #__PURE__ */ (function () {
    function NestedLevel() {

    };
    NestedLevel.value = new NestedLevel();
    return NestedLevel;
})();
var GenieSilhouette = /* #__PURE__ */ (function () {
    function GenieSilhouette() {

    };
    GenieSilhouette.value = new GenieSilhouette();
    return GenieSilhouette;
})();
var ConvexAbsorb = /* #__PURE__ */ (function () {
    function ConvexAbsorb() {

    };
    ConvexAbsorb.value = new ConvexAbsorb();
    return ConvexAbsorb;
})();
var yLiftStep = function (p) {
    return function (isEnd) {
        return function (params) {
            return function (h) {
                if (isEnd) {
                    return {
                        prng: p,
                        yLift: 0.0
                    };
                };
                if (Data_Boolean.otherwise) {
                    var rn = Markgraf_Whiteboard_Prng.next(p);
                    return {
                        prng: rn.prng,
                        yLift: rn.value * params.yJitter * h
                    };
                };
                throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 893, column 1 - line 893, column 91): " + [ p.constructor.name, isEnd.constructor.name, params.constructor.name, h.constructor.name ]);
            };
        };
    };
};
var xJitterStep = function (p) {
    return function (isEnd) {
        return function (params) {
            return function (stepPx) {
                if (isEnd) {
                    return {
                        prng: p,
                        dx: 0.0
                    };
                };
                if (Data_Boolean.otherwise) {
                    var rn = Markgraf_Whiteboard_Prng.next(p);
                    return {
                        prng: rn.prng,
                        dx: (rn.value * 2.0 - 1.0) * params.xJitter * stepPx
                    };
                };
                throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 900, column 1 - line 900, column 90): " + [ p.constructor.name, isEnd.constructor.name, params.constructor.name, stepPx.constructor.name ]);
            };
        };
    };
};
var upperIntersect = function (c0) {
    return function (c1) {
        var dy = c1.cy - c0.cy;
        var dx = c1.cx - c0.cx;
        var d = Data_Number.sqrt(dx * dx + dy * dy);
        var a = ((c0.r * c0.r - c1.r * c1.r) + d * d) / (2.0 * d);
        var hh = Data_Number.sqrt(max(0.0)(c0.r * c0.r - a * a));
        var mx = c0.cx + (a * dx) / d;
        var my = c0.cy + (a * dy) / d;
        var p1 = {
            x: mx - (hh * dy) / d,
            y: my + (hh * dx) / d
        };
        var p2 = {
            x: mx + (hh * dy) / d,
            y: my - (hh * dx) / d
        };
        var $64 = d === 0.0 || d > c0.r + c1.r;
        if ($64) {
            return {
                x: (c0.cx + c1.cx) / 2.0,
                y: c0.cy
            };
        };
        var $65 = p1.y < p2.y;
        if ($65) {
            return p1;
        };
        return p2;
    };
};
var unRunDraw = function (v) {
    return v;
};
var strokePath = function (dict) {
    return dict.strokePath;
};
var shiftFor = function (isHero) {
    return function (isSmall) {
        return function (params) {
            return function (stepPx) {
                if (isHero) {
                    return params.heroShift * stepPx;
                };
                if (isSmall) {
                    return -1.0 * params.smallShift * stepPx;
                };
                if (Data_Boolean.otherwise) {
                    return 0.0;
                };
                throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 907, column 1 - line 907, column 66): " + [ isHero.constructor.name, isSmall.constructor.name, params.constructor.name, stepPx.constructor.name ]);
            };
        };
    };
};
var setViewport = function (dict) {
    return dict.setViewport;
};
var roundedRectPath = function (pos) {
    return function (r) {
        var rx = min(r)(min(pos.w / 2.0)(pos.h / 2.0));
        return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x + rx, pos.y, Markgraf_Animation_Render_Op.pathOpLine, (pos.x + pos.w) - rx, pos.y, Markgraf_Animation_Render_Op.pathOpQuad, pos.x + pos.w, pos.y, pos.x + pos.w, pos.y + rx, Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, (pos.y + pos.h) - rx, Markgraf_Animation_Render_Op.pathOpQuad, pos.x + pos.w, pos.y + pos.h, (pos.x + pos.w) - rx, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpLine, pos.x + rx, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpQuad, pos.x, pos.y + pos.h, pos.x, (pos.y + pos.h) - rx, Markgraf_Animation_Render_Op.pathOpLine, pos.x, pos.y + rx, Markgraf_Animation_Render_Op.pathOpQuad, pos.x, pos.y, pos.x + rx, pos.y, Markgraf_Animation_Render_Op.pathOpClose ];
    };
};
var radiusStep = function (p) {
    return function (isEnd) {
        return function (isHero) {
            return function (isSmall) {
                return function (baseR) {
                    return function (params) {
                        if (isEnd) {
                            return {
                                prng: p,
                                r: baseR
                            };
                        };
                        if (isHero) {
                            return {
                                prng: p,
                                r: baseR * (1.0 + params.rJitter)
                            };
                        };
                        if (isSmall) {
                            return {
                                prng: p,
                                r: baseR * params.smallScale
                            };
                        };
                        if (Data_Boolean.otherwise) {
                            var rn = Markgraf_Whiteboard_Prng.next(p);
                            return {
                                prng: rn.prng,
                                r: baseR * (1.0 + rn.value * params.rJitter)
                            };
                        };
                        throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 884, column 1 - line 884, column 110): " + [ p.constructor.name, isEnd.constructor.name, isHero.constructor.name, isSmall.constructor.name, baseR.constructor.name, params.constructor.name ]);
                    };
                };
            };
        };
    };
};
var pushTransform = function (dict) {
    return dict.pushTransform;
};
var pushLayer = function (dict) {
    return dict.pushLayer;
};
var pushClip = function (dict) {
    return dict.pushClip;
};
var pushBlur = function (dict) {
    return dict.pushBlur;
};
var pushBlend = function (dict) {
    return dict.pushBlend;
};
var pushBakedTransform = function (dict) {
    return dict.pushBakedTransform;
};
var pushAlpha = function (dict) {
    return dict.pushAlpha;
};
var popTransform = function (dict) {
    return dict.popTransform;
};
var popLayer = function (dict) {
    return dict.popLayer;
};
var popClip = function (dict) {
    return dict.popClip;
};
var popBlur = function (dict) {
    return dict.popBlur;
};
var popBlend = function (dict) {
    return dict.popBlend;
};
var popBakedTransform = function (dict) {
    return dict.popBakedTransform;
};
var popAlpha = function (dict) {
    return dict.popAlpha;
};
var pillRadius = function (pos) {
    return min(pos.w)(pos.h) / 2.0;
};
var pillPath = function (pos) {
    var r = pillRadius(pos);
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x + r, pos.y, Markgraf_Animation_Render_Op.pathOpLine, (pos.x + pos.w) - r, pos.y, Markgraf_Animation_Render_Op.pathOpCube, ((pos.x + pos.w) - r) + 0.5522847498307936 * r, pos.y, pos.x + pos.w, (pos.y + r) - 0.5522847498307936 * r, pos.x + pos.w, pos.y + r, Markgraf_Animation_Render_Op.pathOpCube, pos.x + pos.w, pos.y + r + 0.5522847498307936 * r, ((pos.x + pos.w) - r) + 0.5522847498307936 * r, pos.y + pos.h, (pos.x + pos.w) - r, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpLine, pos.x + r, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpCube, (pos.x + r) - 0.5522847498307936 * r, pos.y + pos.h, pos.x, pos.y + r + 0.5522847498307936 * r, pos.x, pos.y + r, Markgraf_Animation_Render_Op.pathOpCube, pos.x, (pos.y + r) - 0.5522847498307936 * r, (pos.x + r) - 0.5522847498307936 * r, pos.y, pos.x + r, pos.y, Markgraf_Animation_Render_Op.pathOpClose ];
};
var pickWithDenom = function (p) {
    return function (arr) {
        return function (denom) {
            var r = Markgraf_Whiteboard_Prng.next(p);
            var v = Data_Array.length(arr);
            if (v === 0) {
                return {
                    prng: p,
                    idx: -1 | 0
                };
            };
            return {
                prng: r.prng,
                idx: Data_Maybe.fromMaybe(-1 | 0)(Data_Array.index(arr)(clamp(0)(v - 1 | 0)(Data_Int.floor(r.value * Data_Int.toNumber(denom)))))
            };
        };
    };
};
var pickFromArray = function (p) {
    return function (arr) {
        var r = Markgraf_Whiteboard_Prng.next(p);
        var v = Data_Array.length(arr);
        if (v === 0) {
            return {
                prng: p,
                idx: -1 | 0
            };
        };
        return {
            prng: r.prng,
            idx: Data_Maybe.fromMaybe(-1 | 0)(Data_Array.index(arr)(clamp(0)(v - 1 | 0)(Data_Int.floor(r.value * Data_Int.toNumber(v)))))
        };
    };
};
var parallelogramSkew = function (pos) {
    return min(pos.w * 0.18)(pos.h * 0.6);
};
var parallelogramPath = function (pos) {
    var skew = parallelogramSkew(pos);
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x + skew, pos.y, Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, pos.y, Markgraf_Animation_Render_Op.pathOpLine, (pos.x + pos.w) - skew, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpLine, pos.x, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpClose ];
};
var monadRunDraw = Run.monadRun;
var measureText = function (dict) {
    return dict.measureText;
};
var measureInk = function (dict) {
    return dict.measureInk;
};
var k = 0.5522847498;
var insideTokenStyle = function (dict) {
    return dict.insideTokenStyle;
};
var functorRunDraw = Run.functorRun;
var fillStrokePath = function (dict) {
    return dict.fillStrokePath;
};
var fillPath = function (dict) {
    return dict.fillPath;
};
var eqLevelNesting = {
    eq: function (x) {
        return function (y) {
            if (x instanceof FlatLevel && y instanceof FlatLevel) {
                return true;
            };
            if (x instanceof NestedLevel && y instanceof NestedLevel) {
                return true;
            };
            return false;
        };
    }
};
var eqInsideTokenStyle = {
    eq: function (x) {
        return function (y) {
            if (x instanceof GenieSilhouette && y instanceof GenieSilhouette) {
                return true;
            };
            if (x instanceof ConvexAbsorb && y instanceof ConvexAbsorb) {
                return true;
            };
            return false;
        };
    }
};
var drawTextAffine = function (dict) {
    return dict.drawTextAffine;
};
var drawText = function (dict) {
    return dict.drawText;
};
var drawRoundedRect = function (dict) {
    return dict.drawRoundedRect;
};
var drawClosedPath = function (dictMonadDraw) {
    var fillStrokePath1 = fillStrokePath(dictMonadDraw);
    var fillPath1 = fillPath(dictMonadDraw);
    var strokePath1 = strokePath(dictMonadDraw);
    var pure1 = Control_Applicative.pure((dictMonadDraw.Monad0()).Applicative0());
    return function (p) {
        return function (mFill) {
            return function (mStroke) {
                if (mFill instanceof Data_Maybe.Just && mStroke instanceof Data_Maybe.Just) {
                    return fillStrokePath1(p)(mFill.value0)(mStroke.value0);
                };
                if (mFill instanceof Data_Maybe.Just && mStroke instanceof Data_Maybe.Nothing) {
                    return fillPath1(p)(mFill.value0);
                };
                if (mFill instanceof Data_Maybe.Nothing && mStroke instanceof Data_Maybe.Just) {
                    return strokePath1(p)(mStroke.value0);
                };
                if (mFill instanceof Data_Maybe.Nothing && mStroke instanceof Data_Maybe.Nothing) {
                    return pure1(Data_Unit.unit);
                };
                throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 287, column 34 - line 291, column 32): " + [ mFill.constructor.name, mStroke.constructor.name ]);
            };
        };
    };
};
var documentPath = function (pos) {
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, pos.y, Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, pos.y, Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, pos.y + pos.h * 0.85, Markgraf_Animation_Render_Op.pathOpCube, pos.x + pos.w * 0.66, pos.y + pos.h * 1.05, pos.x + pos.w * 0.34, pos.y + pos.h * 0.65, pos.x, pos.y + pos.h * 0.85, Markgraf_Animation_Render_Op.pathOpClose ];
};
var defaultCloudParams = {
    rBase: 1.2,
    stepRatio: 0.7,
    rJitter: 0.35,
    yJitter: 0.2,
    xJitter: 0.2,
    heroShift: 0.2,
    smallScale: 0.85,
    smallShift: 0.15,
    seed: 7,
    minN: 3
};
var cylinderRy = function (pos) {
    return min(pos.h * 7.5e-2)(pos.w * 7.5e-2);
};
var cylinderTopEllipsePath = function (pos) {
    var ry = cylinderRy(pos);
    var rx = pos.w / 2.0;
    var cyTop = pos.y + ry;
    var cx = pos.x + rx;
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, cyTop, Markgraf_Animation_Render_Op.pathOpCube, pos.x, cyTop - k * ry, cx - k * rx, pos.y, cx, pos.y, Markgraf_Animation_Render_Op.pathOpCube, cx + k * rx, pos.y, pos.x + pos.w, cyTop - k * ry, pos.x + pos.w, cyTop, Markgraf_Animation_Render_Op.pathOpCube, pos.x + pos.w, cyTop + k * ry, cx + k * rx, cyTop + ry, cx, cyTop + ry, Markgraf_Animation_Render_Op.pathOpCube, cx - k * rx, cyTop + ry, pos.x, cyTop + k * ry, pos.x, cyTop, Markgraf_Animation_Render_Op.pathOpClose ];
};
var cylinderRimBackPath = function (pos) {
    var ry = cylinderRy(pos);
    var rx = pos.w / 2.0;
    var cyBot = (pos.y + pos.h + Markgraf_Graph.cylinderBottomDrop) - ry;
    var cx = pos.x + rx;
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, cyBot, Markgraf_Animation_Render_Op.pathOpCube, pos.x, cyBot - k * ry, cx - k * rx, cyBot - ry, cx, cyBot - ry, Markgraf_Animation_Render_Op.pathOpCube, cx + k * rx, cyBot - ry, pos.x + pos.w, cyBot - k * ry, pos.x + pos.w, cyBot ];
};
var cylinderLipPath = function (pos) {
    var ry = cylinderRy(pos);
    var rx = pos.w / 2.0;
    var cyTop = pos.y + ry;
    var cx = pos.x + rx;
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, cyTop, Markgraf_Animation_Render_Op.pathOpCube, pos.x, cyTop + k * ry, cx - k * rx, cyTop + ry, cx, cyTop + ry, Markgraf_Animation_Render_Op.pathOpCube, cx + k * rx, cyTop + ry, pos.x + pos.w, cyTop + k * ry, pos.x + pos.w, cyTop ];
};
var cylinderLidLensPath = function (pos) {
    var ry = cylinderRy(pos);
    var rx = pos.w / 2.0;
    var cyTop = pos.y + ry;
    var cx = pos.x + rx;
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, cyTop, Markgraf_Animation_Render_Op.pathOpCube, pos.x, cyTop + k * ry, cx - k * rx, cyTop + ry, cx, cyTop + ry, Markgraf_Animation_Render_Op.pathOpCube, cx + k * rx, cyTop + ry, pos.x + pos.w, cyTop + k * ry, pos.x + pos.w, cyTop, Markgraf_Animation_Render_Op.pathOpLine, pos.x, cyTop, Markgraf_Animation_Render_Op.pathOpClose ];
};
var cylinderBottomEllipsePath = function (pos) {
    var ry = cylinderRy(pos);
    var rx = pos.w / 2.0;
    var yBot = pos.y + pos.h + Markgraf_Graph.cylinderBottomDrop;
    var cyBot = yBot - ry;
    var cx = pos.x + rx;
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, cyBot, Markgraf_Animation_Render_Op.pathOpCube, pos.x, cyBot - k * ry, cx - k * rx, cyBot - ry, cx, cyBot - ry, Markgraf_Animation_Render_Op.pathOpCube, cx + k * rx, cyBot - ry, pos.x + pos.w, cyBot - k * ry, pos.x + pos.w, cyBot, Markgraf_Animation_Render_Op.pathOpCube, pos.x + pos.w, cyBot + k * ry, cx + k * rx, yBot, cx, yBot, Markgraf_Animation_Render_Op.pathOpCube, cx - k * rx, yBot, pos.x, cyBot + k * ry, pos.x, cyBot, Markgraf_Animation_Render_Op.pathOpClose ];
};
var cylinderBodyPath = function (pos) {
    var ry = cylinderRy(pos);
    var rx = pos.w / 2.0;
    var yBot = pos.y + pos.h + Markgraf_Graph.cylinderBottomDrop;
    var cyTop = pos.y + ry;
    var cyBot = yBot - ry;
    var cx = pos.x + rx;
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x, cyTop, Markgraf_Animation_Render_Op.pathOpCube, pos.x, cyTop - k * ry, cx - k * rx, pos.y, cx, pos.y, Markgraf_Animation_Render_Op.pathOpCube, cx + k * rx, pos.y, pos.x + pos.w, cyTop - k * ry, pos.x + pos.w, cyTop, Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, cyBot, Markgraf_Animation_Render_Op.pathOpCube, pos.x + pos.w, cyBot + k * ry, cx + k * rx, yBot, cx, yBot, Markgraf_Animation_Render_Op.pathOpCube, cx - k * rx, yBot, pos.x, cyBot + k * ry, pos.x, cyBot, Markgraf_Animation_Render_Op.pathOpClose ];
};
var cloudCircles = function (params) {
    return function (pos) {
        var prng0 = Markgraf_Whiteboard_Prng.mkPrng(params.seed);
        var minNn = Data_Int.toNumber(params.minN);
        var minWidthFactor = 2.0 * (1.0 + (minNn - 1.0) * params.stepRatio);
        var baseline = pos.y + pos.h;
        var baseR = Data_Number.min(params.rBase * pos.h)(pos.w / minWidthFactor);
        var spanW = pos.w - 2.0 * baseR;
        var targetStep = 2.0 * baseR * params.stepRatio;
        var nGuess = (function () {
            if (spanW <= 0.0 || targetStep <= 0.0) {
                return params.minN;
            };
            if (Data_Boolean.otherwise) {
                return Data_Int.round(spanW / targetStep) + 1 | 0;
            };
            throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 760, column 3 - line 762, column 53): " + [  ]);
        })();
        var n = max1(params.minN)(nGuess);
        var interior = (function () {
            var $113 = n >= 3;
            if ($113) {
                return Data_Array.range(1)(n - 2 | 0);
            };
            return [  ];
        })();
        var iLen = Data_Array.length(interior);
        var halfLen = div1(iLen + 1 | 0)(2);
        var leftHalf = Data_Array.take(halfLen)(interior);
        var rightHalf = Data_Array.drop(iLen - halfLen | 0)(interior);
        var hero = pickFromArray(prng0)(rightHalf);
        var smallCandidates = Data_Array.filter(function (v) {
            return v !== hero.idx;
        })(leftHalf);
        var smallDenom = max1(1)(Data_Array.length(leftHalf) - (function () {
            var $114 = elem(hero.idx)(leftHalf);
            if ($114) {
                return 1;
            };
            return 0;
        })() | 0);
        var small = pickWithDenom(hero.prng)(smallCandidates)(smallDenom);
        var initAcc = {
            prng: small.prng,
            circles: [  ]
        };
        var stepPx = (function () {
            var $115 = n >= 2;
            if ($115) {
                return spanW / (Data_Int.toNumber(n) - 1.0);
            };
            return 0.0;
        })();
        var step = function (acc) {
            return function (i) {
                var t = (function () {
                    var $116 = n >= 2;
                    if ($116) {
                        return Data_Int.toNumber(i) / (Data_Int.toNumber(n) - 1.0);
                    };
                    return 0.0;
                })();
                var isSmall = i === small.idx;
                var isHero = i === hero.idx;
                var isEnd = i === 0 || i === (n - 1 | 0);
                var rR = radiusStep(acc.prng)(isEnd)(isHero)(isSmall)(baseR)(params);
                var yR = yLiftStep(rR.prng)(isEnd)(params)(pos.h);
                var xR = xJitterStep(yR.prng)(isEnd)(params)(stepPx);
                var cxLocal = baseR + t * spanW + xR.dx + shiftFor(isHero)(isSmall)(params)(stepPx);
                var cxClamped = clamp1(rR.r)(pos.w - rR.r)(cxLocal);
                var c = {
                    cx: pos.x + cxClamped,
                    cy: baseline - yR.yLift,
                    r: rR.r
                };
                return {
                    prng: xR.prng,
                    circles: Data_Array.snoc(acc.circles)(c)
                };
            };
        };
        var built = Data_Array.foldl(step)(initAcc)(Data_Array.range(0)(n - 1 | 0));
        return built.circles;
    };
};
var cloudArcEndpoints = function (circles) {
    return function (baseline) {
        var rightBase = function (c) {
            return {
                x: c.cx + c.r,
                y: baseline
            };
        };
        var n = Data_Array.length(circles);
        var leftBase = function (c) {
            return {
                x: c.cx - c.r,
                y: baseline
            };
        };
        var go = function (i) {
            return function (c) {
                return {
                    c: c,
                    p1: (function () {
                        var $117 = i === 0;
                        if ($117) {
                            return leftBase(c);
                        };
                        var v = Data_Array.index(circles)(i - 1 | 0);
                        if (v instanceof Data_Maybe.Just) {
                            return upperIntersect(v.value0)(c);
                        };
                        if (v instanceof Data_Maybe.Nothing) {
                            return leftBase(c);
                        };
                        throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 805, column 14 - line 807, column 32): " + [ v.constructor.name ]);
                    })(),
                    p2: (function () {
                        var $120 = i === (n - 1 | 0);
                        if ($120) {
                            return rightBase(c);
                        };
                        var v = Data_Array.index(circles)(i + 1 | 0);
                        if (v instanceof Data_Maybe.Just) {
                            return upperIntersect(c)(v.value0);
                        };
                        if (v instanceof Data_Maybe.Nothing) {
                            return rightBase(c);
                        };
                        throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 810, column 14 - line 812, column 33): " + [ v.constructor.name ]);
                    })()
                };
            };
        };
        return Data_Array.mapWithIndex(go)(circles);
    };
};
var clearBackground = function (dict) {
    return dict.clearBackground;
};
var chevronWidth = function (pos) {
    return min(pos.h * 0.4)(pos.w * 0.2);
};
var chevronRectPath = function (pos) {
    var chev = chevronWidth(pos);
    return [ Markgraf_Animation_Render_Op.pathOpMove, pos.x + chev, pos.y, Markgraf_Animation_Render_Op.pathOpLine, (pos.x + pos.w) - chev, pos.y, Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, pos.y + pos.h / 2.0, Markgraf_Animation_Render_Op.pathOpLine, (pos.x + pos.w) - chev, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpLine, pos.x + chev, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpLine, pos.x, pos.y + pos.h / 2.0, Markgraf_Animation_Render_Op.pathOpClose ];
};
var bindRunDraw = Run.bindRun;
var backgroundDots = function (dict) {
    return dict.backgroundDots;
};
var arcOverTopCubics = function (c) {
    return function (p1) {
        return function (p2) {
            var thetaStart = Data_Number.atan2(p1.y - c.cy)(p1.x - c.cx);
            var thetaEndRaw = Data_Number.atan2(p2.y - c.cy)(p2.x - c.cx);
            var thetaEnd = (function () {
                var $125 = thetaEndRaw > thetaStart;
                if ($125) {
                    return thetaEndRaw;
                };
                return thetaEndRaw + 2.0 * Data_Number.pi;
            })();
            var totalSpan = thetaEnd - thetaStart;
            var numSegs = max1(1)(Data_Int.ceil(totalSpan / (Data_Number.pi / 2.0)));
            var segSpan = totalSpan / Data_Int.toNumber(numSegs);
            var alpha = (4.0 / 3.0) * Data_Number.tan(segSpan / 4.0);
            var emit = function (k1) {
                var a1 = thetaStart + Data_Int.toNumber(k1 + 1 | 0) * segSpan;
                var ex = c.cx + c.r * Data_Number.cos(a1);
                var ey = c.cy + c.r * Data_Number.sin(a1);
                var a0 = thetaStart + Data_Int.toNumber(k1) * segSpan;
                var sx = c.cx + c.r * Data_Number.cos(a0);
                var sy = c.cy + c.r * Data_Number.sin(a0);
                return [ Markgraf_Animation_Render_Op.pathOpCube, sx - alpha * c.r * Data_Number.sin(a0), sy + alpha * c.r * Data_Number.cos(a0), ex + alpha * c.r * Data_Number.sin(a1), ey - alpha * c.r * Data_Number.cos(a1), ex, ey ];
            };
            return Data_Array.concatMap(emit)(Data_Array.range(0)(numSegs - 1 | 0));
        };
    };
};
var cloudPath = function (pos) {
    return function (cornerRaw) {
        var hatH = pos.h * Markgraf_Graph.cloudHatRatio;
        var hatRect = {
            x: pos.x,
            y: pos.y - hatH,
            w: pos.w,
            h: hatH
        };
        var segs = cloudArcEndpoints(cloudCircles(defaultCloudParams)(hatRect))(pos.y);
        var rx = min(cornerRaw)(min(pos.w / 2.0)(pos.h / 2.0));
        var arcCmds = function (seg) {
            return arcOverTopCubics(seg.c)(seg.p1)(seg.p2);
        };
        var v = Data_Array.head(segs);
        if (v instanceof Data_Maybe.Nothing) {
            return roundedRectPath(pos)(cornerRaw);
        };
        if (v instanceof Data_Maybe.Just) {
            return append([ Markgraf_Animation_Render_Op.pathOpMove, v.value0.p1.x, v.value0.p1.y ])(append(Data_Array.concatMap(arcCmds)(segs))([ Markgraf_Animation_Render_Op.pathOpLine, pos.x + pos.w, (pos.y + pos.h) - rx, Markgraf_Animation_Render_Op.pathOpQuad, pos.x + pos.w, pos.y + pos.h, (pos.x + pos.w) - rx, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpLine, pos.x + rx, pos.y + pos.h, Markgraf_Animation_Render_Op.pathOpQuad, pos.x, pos.y + pos.h, pos.x, (pos.y + pos.h) - rx, Markgraf_Animation_Render_Op.pathOpLine, v.value0.p1.x, v.value0.p1.y, Markgraf_Animation_Render_Op.pathOpClose ]));
        };
        throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 822, column 27 - line 848, column 10): " + [ v.constructor.name ]);
    };
};
var drawShape = function (dictMonadDraw) {
    var drawRoundedRect1 = drawRoundedRect(dictMonadDraw);
    var drawClosedPath1 = drawClosedPath(dictMonadDraw);
    return function (v) {
        return function (v1) {
            return function (v2) {
                return function (v3) {
                    return function (v4) {
                        if (v instanceof Markgraf_Graph.Rectangle) {
                            return drawRoundedRect1(v1)(v2)(v3)(v4);
                        };
                        if (v instanceof Markgraf_Graph.Cylinder) {
                            return drawClosedPath1(cylinderBodyPath(v1))(v3)(v4);
                        };
                        if (v instanceof Markgraf_Graph.Parallelogram) {
                            return drawClosedPath1(parallelogramPath(v1))(v3)(v4);
                        };
                        if (v instanceof Markgraf_Graph.Diamond) {
                            return drawClosedPath1(chevronRectPath(v1))(v3)(v4);
                        };
                        if (v instanceof Markgraf_Graph.Ellipse) {
                            return drawClosedPath1(pillPath(v1))(v3)(v4);
                        };
                        if (v instanceof Markgraf_Graph.Document) {
                            return drawClosedPath1(documentPath(v1))(v3)(v4);
                        };
                        if (v instanceof Markgraf_Graph.Cloud) {
                            return drawClosedPath1(cloudPath(v1)(v2))(v3)(v4);
                        };
                        return drawRoundedRect1(v1)(v2)(v3)(v4);
                    };
                };
            };
        };
    };
};
var applyRunDraw = Run.applyRun;
var applicativeRunDraw = Run.applicativeRun;
var pure = /* #__PURE__ */ Control_Applicative.pure(applicativeRunDraw);
var monadDrawRunDraw = /* #__PURE__ */ (function () {
    return {
        fillPath: function (p) {
            return function (s) {
                return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.FillPath(p, s, Data_Unit.unit));
            };
        },
        strokePath: function (p) {
            return function (s) {
                return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.StrokePath(p, s, Data_Unit.unit));
            };
        },
        fillStrokePath: function (p) {
            return function (f) {
                return function (s) {
                    return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.FillStrokePath(p, f, s, Data_Unit.unit));
                };
            };
        },
        drawRoundedRect: function (rect) {
            return function (r) {
                return function (mFill) {
                    return function (mStroke) {
                        if (mFill instanceof Data_Maybe.Just && mStroke instanceof Data_Maybe.Just) {
                            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.FillStrokePath(roundedRectPath(rect)(r), mFill.value0, mStroke.value0, Data_Unit.unit));
                        };
                        if (mFill instanceof Data_Maybe.Just && mStroke instanceof Data_Maybe.Nothing) {
                            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.FillPath(roundedRectPath(rect)(r), mFill.value0, Data_Unit.unit));
                        };
                        if (mFill instanceof Data_Maybe.Nothing && mStroke instanceof Data_Maybe.Just) {
                            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.StrokePath(roundedRectPath(rect)(r), mStroke.value0, Data_Unit.unit));
                        };
                        if (mFill instanceof Data_Maybe.Nothing && mStroke instanceof Data_Maybe.Nothing) {
                            return pure(Data_Unit.unit);
                        };
                        throw new Error("Failed pattern match at Markgraf.Animation.Render.Draw (line 185, column 42 - line 189, column 34): " + [ mFill.constructor.name, mStroke.constructor.name ]);
                    };
                };
            };
        },
        drawText: function (spec) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.DrawText(spec, Data_Unit.unit));
        },
        drawTextAffine: function (af) {
            return function (spec) {
                return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.DrawTextAffine(af, spec, Data_Unit.unit));
            };
        },
        pushTransform: function (t) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushTransform(t, Data_Unit.unit));
        },
        popTransform: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopTransform(Data_Unit.unit)),
        pushBakedTransform: function (t) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushTransform(t, Data_Unit.unit));
        },
        popBakedTransform: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopTransform(Data_Unit.unit)),
        pushClip: function (p) {
            return function (rule) {
                return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushClip(p, rule, Data_Unit.unit));
            };
        },
        popClip: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopClip(Data_Unit.unit)),
        pushBlend: function (m) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushBlend(m, Data_Unit.unit));
        },
        popBlend: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopBlend(Data_Unit.unit)),
        pushAlpha: function (a) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushAlpha(a, Data_Unit.unit));
        },
        popAlpha: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopAlpha(Data_Unit.unit)),
        pushBlur: function (radius) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushBlur(radius, Data_Unit.unit));
        },
        popBlur: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopBlur(Data_Unit.unit)),
        pushLayer: function (k1) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PushLayer(k1, Data_Unit.unit));
        },
        popLayer: lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.PopLayer(Data_Unit.unit)),
        setViewport: function (vp) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.SetViewport(vp, Data_Unit.unit));
        },
        clearBackground: function (c) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.ClearBackground(c, Data_Unit.unit));
        },
        backgroundDots: function (spec) {
            return lift(Markgraf_Animation_Render_Op["_render"])(new Markgraf_Animation_Render_Op.BackgroundDots(spec, Data_Unit.unit));
        },
        measureText: function (font) {
            return function (text) {
                return Markgraf_Animation_Render_Metrics.measureText(font)(text);
            };
        },
        measureInk: function (font) {
            return function (text) {
                return Markgraf_Animation_Render_Metrics.measureInk(font)(text);
            };
        },
        insideTokenStyle: function (v) {
            return pure(GenieSilhouette.value);
        },
        Monad0: function () {
            return monadRunDraw;
        }
    };
})();
export {
    fillPath,
    strokePath,
    fillStrokePath,
    drawRoundedRect,
    drawShape,
    drawText,
    drawTextAffine,
    pushTransform,
    popTransform,
    pushBakedTransform,
    popBakedTransform,
    pushClip,
    popClip,
    pushBlend,
    popBlend,
    pushAlpha,
    popAlpha,
    pushBlur,
    popBlur,
    pushLayer,
    popLayer,
    setViewport,
    clearBackground,
    backgroundDots,
    measureText,
    measureInk,
    insideTokenStyle,
    roundedRectPath,
    cylinderBodyPath,
    cylinderBottomEllipsePath,
    cylinderLidLensPath,
    cylinderLipPath,
    cylinderRimBackPath,
    cylinderRy,
    cylinderTopEllipsePath,
    parallelogramPath,
    parallelogramSkew,
    chevronRectPath,
    chevronWidth,
    pillPath,
    pillRadius,
    documentPath,
    cloudPath,
    cloudCircles,
    cloudArcEndpoints,
    defaultCloudParams,
    RunDraw,
    unRunDraw,
    FlatLevel,
    NestedLevel,
    GenieSilhouette,
    ConvexAbsorb,
    eqLevelNesting,
    eqInsideTokenStyle,
    functorRunDraw,
    applyRunDraw,
    applicativeRunDraw,
    bindRunDraw,
    monadRunDraw,
    monadDrawRunDraw
};
//# sourceMappingURL=index.js.map
